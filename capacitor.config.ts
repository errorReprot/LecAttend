import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lectureattendancetracker.app',
  appName: 'Lecture Attendance Tracker',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;